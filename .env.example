# =============================================================================
# az-scout – Environment Variables
# =============================================================================

# --- Authentication Mode ---
# "entra"  → Real Entra ID authentication (requires AZURE_* vars below)
# "mock"   → Bypass auth for local development (no Azure config needed)
AUTH_MODE=entra

# --- Entra ID App Registration ---
# Required when AUTH_MODE=entra

# Directory (tenant) ID from the Azure portal App Registration overview
AUTH_TENANT_ID=

# Application (client) ID from the Azure portal App Registration overview
AUTH_CLIENT_ID=

# Client secret value created under Certificates & secrets
AUTH_CLIENT_SECRET=

# The API scope exposed by your App Registration (Expose an API → add scope)
# Format: api://<client_id>/access_as_user
AUTH_API_SCOPE=api://<client_id>/access_as_user

# --- Server ---
HOST=0.0.0.0
PORT=5001
